import { FilterEstadoCalidadPipe } from './filter-estado-calidad.pipe';

describe('FilterEstadoCalidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEstadoCalidadPipe();
    expect(pipe).toBeTruthy();
  });
});
