import { FilterNumeroPipe } from './filter-numero.pipe';

describe('FilterNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
