import { FilterReferenciaPipe } from './filter-referencia.pipe';

describe('FilterReferenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReferenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
