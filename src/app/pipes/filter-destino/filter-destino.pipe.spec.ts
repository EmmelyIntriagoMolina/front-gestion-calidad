import { FilterDestinoPipe } from './filter-destino.pipe';

describe('FilterDestinoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDestinoPipe();
    expect(pipe).toBeTruthy();
  });
});
