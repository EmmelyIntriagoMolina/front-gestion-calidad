import { FilterLotePipe } from './filter-lote.pipe';

describe('FilterLotePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLotePipe();
    expect(pipe).toBeTruthy();
  });
});
